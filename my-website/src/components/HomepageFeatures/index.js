import clsx from 'clsx';
import Heading from '@theme/Heading';
import styles from './styles.module.css';

// This component is deprecated. Use the TextbookFeatures component in src/pages/index.js instead.
const FeatureList = [];

export default function HomepageFeatures() {
  // This is an empty component to maintain compatibility if referenced elsewhere
  return null;
}
